{
  "name": "rack-em-up",
  "private": true,
  "description": "RackUp monorepo. Apps live in rackup-backend and rackup-web. Do not install Express or TypeScript at the root.",
  "scripts": {
    "backend": "npm --prefix rackup-backend start",
    "backend:dev": "npm --prefix rackup-backend run start:dev",
    "web": "npm --prefix rackup-web run dev"
  }
}
